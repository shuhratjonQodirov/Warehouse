spring:
  application:
    name: Warehouse
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/warehouse
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never
